# Examples

Plain, runnable demonstration scripts ported from `org.metacsp.examples.*`.
Each file is standalone: `python examples/<name>.py`. These are demos for
newcomers, not tests — the test suite lives in `tests/`.
